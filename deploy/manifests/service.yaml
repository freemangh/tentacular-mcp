apiVersion: v1
kind: Service
metadata:
  name: tentacular-mcp
  namespace: tentacular-system
  labels:
    app.kubernetes.io/name: tentacular-mcp
    app.kubernetes.io/component: server
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: tentacular-mcp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
