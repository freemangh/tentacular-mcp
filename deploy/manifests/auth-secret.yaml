# Generate the token value with: openssl rand -hex 32
apiVersion: v1
kind: Secret
metadata:
  name: tentacular-mcp-auth
  namespace: tentacular-system
type: Opaque
stringData:
  token: "REPLACE_ME_WITH_GENERATED_TOKEN"
